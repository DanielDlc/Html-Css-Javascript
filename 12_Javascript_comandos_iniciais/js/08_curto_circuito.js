/* 
&& -> false && true -> false " o valor é o mesmo"
|| ->
false  literal
valores avaliádos em falsy (false, 0, ' ', " ", ` `, null, undefined, NaN)
*/

// && true significa que todas expressões são veradeira e me retorna o último verdadeiro.
console.log('Daniel Costa' && true && 'Bianca');

// || -> or precisa de apenas um para retornar verdadeiro.
console.log(0 || false || null || 'Daniel Costa' || True);  // retornou o primeiro valor verdadeiro.










